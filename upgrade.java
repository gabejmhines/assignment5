public class upgrade {
    //    public static String belowAverage(String averageStudents){
//        //input into array (if for) then sort into order
//        int[] aboveAverageGrades = new int[100]; //this holds all of the ints input by the user
//
//        for (int z = 0; z < averageStudents.length(); z++) {
//            aboveAverageGrades[z] = averageStudents.charAt(z);
//        } //adds all to array
//        Arrays.sort(aboveAverageGrades);
//        //for loop going forward through sorted list of user inputs every time 0
//        //if any previous input is below new average
//        //then ++j to change the index location of the sorted selected array printer to print up until the later value after new
//        for(int j = 100; j > aboveAverageGrades.length; j--){
//            int check = aboveAverageGrades[j];
//            if(check < averageStudent){
//                ++doorCheck;
//            }
//        }
//        while(doorCheck < averageStudents.length()){
//            truth += aboveAverageGrades[averageStudents.length()];
//            doorCheck++;
//        }
//
//
//    }
}
